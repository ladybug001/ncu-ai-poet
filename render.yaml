services:
  - type: web
    name: ai-poet-studio
    env: python
    region: singapore
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    # API_KEY 等机密信息请在 Render 后台 Environment 中手动添加
